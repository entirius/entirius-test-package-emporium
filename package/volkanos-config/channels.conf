# channels.conf — Declares channels and their currencies for import
# Format: channel:currency (one per line)
# Lines starting with # are comments, blank lines are ignored
#
# Used by import-package.sh to drive all channel-dependent import steps.
# Override at runtime via IMPORT_CHANNELS env var (comma-separated channel:currency pairs).

default-local:USD
default-europe:EUR
